#import "Basic";

render_cube :: (name: string) -> bool {
    name := "Patrik";
    age := 23;

    Person :: struct {
        name: string;
        age: int;
    }

    render_in_render :: () {
        blah := 10;
    }
}

